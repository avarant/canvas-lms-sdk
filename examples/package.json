{
  "name": "canvas-lms-sdk-examples",
  "version": "1.0.0",
  "description": "Examples for Canvas LMS SDK",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}